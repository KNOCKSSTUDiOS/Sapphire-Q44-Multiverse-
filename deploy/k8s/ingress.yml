# NEBULA Multiverse Studio — Ingress (OPTIONAL / COMMENTED) | Architect: KNOCKS
# Uncomment after installing ingress-nginx + cert-manager and replacing domain.
# See deploy/NOTES.md §6 for full setup instructions.

# apiVersion: networking.k8s.io/v1
# kind: Ingress
# metadata:
#   name: nebula-ingress
#   namespace: nebula
#   annotations:
#     kubernetes.io/ingress.class: "nginx"
#     nginx.ingress.kubernetes.io/ssl-redirect: "true"
#     nginx.ingress.kubernetes.io/force-ssl-redirect: "true"
#     nginx.ingress.kubernetes.io/proxy-body-size: "16m"
#     cert-manager.io/cluster-issuer: "letsencrypt-prod"
# spec:
#   tls:
#     - hosts:
#         - REPLACE_WITH_YOUR_DOMAIN
#       secretName: nebula-tls-cert
#   rules:
#     - host: REPLACE_WITH_YOUR_DOMAIN
#       http:
#         paths:
#           - path: /
#             pathType: Prefix
#             backend:
#               service:
#                 name: nebula-frontend-svc
#                 port:
#                   number: 80
#           # Optional direct backend exposure (WebSocket, gRPC, etc.)
#           # - path: /api
#           #   pathType: Prefix
#           #   backend:
#           #     service:
#           #       name: nebula-backend-svc
#           #       port:
#           #         number: 3000
