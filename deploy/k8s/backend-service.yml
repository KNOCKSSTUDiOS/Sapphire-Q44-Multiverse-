# NEBULA Multiverse Studio — Backend Service | Architect: KNOCKS
apiVersion: v1
kind: Service
metadata:
  name: nebula-backend-svc
  namespace: nebula
  labels:
    app: nebula-backend
    app.kubernetes.io/part-of: nebula-multiverse-studio
    app.kubernetes.io/managed-by: knocks-architect
spec:
  type: ClusterIP
  selector:
    app: nebula-backend
  ports:
    - name: http
      protocol: TCP
      port: 3000
      targetPort: 3000
